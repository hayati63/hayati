# بازبینی `dashboard_monthly.py` و باکس حجمی — شهریور ۱۴۰۵

مرجع: خروجی اجرای ۲۰۲۶/۰۹/۲۱، فایل‌های `app/dash.html`، چهار اسکریپت بک‌تست،
`pinescript/lvn_clarity_cascade.pine` و `CLAUDE.md`.

---

## ۱. عدد ۷۷٫۹٪ چیزی را اندازه نمی‌گیرد

```
معاملات بک‌تست: 615
موفقیت: 479/615 = 77.9%
میانگین بازده: +12.34%
```

سه دلیل که این عدد قابل استفاده نیست:

### الف) نرخ پایه غایب است

قاعدهٔ ۲ در `CLAUDE.md`: «هر عدد بک‌تست باید با نرخ پایه یا گروه کنترل مقایسه
شود.» هیچ‌کدام از چهار اسکریپت و خود `dashboard_monthly.py` نرخ پایه حساب
نمی‌کنند.

از خروجی `export_monthly_risk.py` (۱۴۲ نماد) شمردم:

| | سبز | داخل | قرمز |
|---|---|---|---|
| ماه جاری | ۸۶ (۶۰٫۶٪) | ۲۸ (۱۹٫۷٪) | ۲۸ (۱۹٫۷٪) |
| **ماه قبل** | **۱۳۷ (۹۸٫۶٪)** | ۲ (۱٫۴٪) | **صفر** |

میانهٔ «ریسک» ماه قبل برای سبزها: ۱۱٫۳۷٪.

**ماه قبل ۹۸٫۶٪ کل جهان نماد بالای باکسش بسته.** سیگنالی که روی ۹۸٫۶٪ نمادها
روشن می‌شود فیلتر نیست؛ یک شرط‌بندی روی جهت کل بازار است. بک‌تست دقیقاً روی
چنین ماه‌هایی گرفته شده، پس ۷۷٫۹٪ احتمالاً برابر نرخ پایه است، نه بالاتر از آن.

### ب) n واقعی ۶۱۵ نیست، حدود ۶ است

دانلود با `n=150` کندل روزانه بسته می‌شود — یعنی حدود ۷ ماه معاملاتی. خروجی هم
همین را می‌گوید: «۴ معامله»، «۵ معامله»، «۶ معامله» به ازای هر نماد.

۶۱۵ معامله = ۱۴۵ نماد × ~۴٫۲ ماه. این ۱۴۵ نماد **هم‌زمان یک بازار را** معامله
می‌کنند؛ بازده‌شان به‌شدت همبسته است. تعداد مشاهدهٔ مستقل تعداد **ماه** است، نه
تعداد معامله. با ۶ ماه هیچ ادعای آماری‌ای ممکن نیست.

نمادهایی با دادهٔ تقریباً صفر هم وارد استخر شده‌اند: نقرآمد `n=9`، یاس `n=11`،
بانکپاد `n=11`، سیلوا `n=12`، سیمان `n=17`، سیان `n=18`. سه نماد با «۱ معامله»
(دلتا، مهرگلد، پلاتا) در میانگین ۶۱۵تایی وزن گرفته‌اند.

### ج) صندوق درآمد ثابت برگشته داخل جهان نماد

`backtest_auto_nofixed.py` فهرست `FIXED_KEYS` دارد تا این‌ها را کنار بگذارد. در
اجرای جدید همان‌ها حاضرند: گارانتی، ضمان، زمرد، هیبرید، تمشک، اعتبارسهام،
کاردان، مختلط، کلید، همسنگ.

قیمت صندوق درآمد ثابت تقریباً یکنواخت بالا می‌رود. در هر تست «یک ماه نگه دار»
نزدیک ۱۰۰٪ برد می‌دهد — **مستقل از هر سیگنالی**. حضورشان میانگین را باد می‌کند.

### نمایش عملی

`tools/monthly_backtest.py` را روی دادهٔ **تصادفی ساختگی** اجرا کردم — قدم
تصادفی محض، بدون هیچ سیگنال واقعی، با چند صندوق کم‌نوسان پردریفت داخلش:

```
همه (مثل dashboard_monthly.py)   (n=228)
  نرخ پایه:  68.9٪ مثبت  |  میانگین  +4.08٪
  بالا  n=141  برد  75.9٪ ( +7.0 واحد)  میانگین +5.36٪ (+1.29)  t=+2.97   ← «معنادار»!

بدون درآمد ثابت   (n=171)
  نرخ پایه:  58.5٪ مثبت  |  میانگین  +2.44٪
  بالا  n= 84  برد  59.5٪ ( +1.0 واحد)  میانگین +2.91٪ (+0.47)  t=+0.67   ← هیچ

فقط درآمد ثابت   (n=57)
  نرخ پایه: 100.0٪ مثبت  |  میانگین  +8.98٪
```

روی دادهٔ بی‌معنا، `t=+2.97` بیرون آمد — فقط به‌خاطر مخلوط‌کردن درآمد ثابت و
نبودن نرخ پایه. این همان مکانیزمی است که ۷۷٫۹٪ را می‌سازد.

---

## ۲. باکسی که محاسبه می‌شود، باکس استراتژی نیست

این مهم‌ترین یافته است. **چهار تعریف متفاوت** در پروژه در جریان است:

| منبع | تعریف | ناحیه |
|---|---|---|
| `CLAUDE.md` بند ۱ | ردیف ۳→۲۰ تطبیقی، **اولین درهٔ حجمی**، دره = مرز باکس | کم‌حجم |
| `lvn_clarity_cascade.pine` | ردیف ۳→۲۰ تطبیقی، اولین کمینهٔ محلی **میانی از پایین** | کم‌حجم |
| `app/dash.html` تابع `box()` | تعداد بین **ثابت**، بین‌بندی روی close، **سه بین پرحجم‌ترین** | **پرحجم** |
| چهار اسکریپت پایتون، `vp_low()` | همان: `nlargest(3)` روی بین‌های close | **پرحجم** |

کامنت خود `dash.html`:

```js
/* ─── باکس حجمی: تعریف خودِ مصطفی، بدون تغییر ───
   کف پایین‌ترینِ سه بینِ پرحجمِ برتر، بین‌بندی روی close، حداکثر ۲۰ بین */
var nb=Math.min(20,Math.max(5,Math.floor(bars.length/2)));
```

سه تناقض با `CLAUDE.md`:

1. **دره در برابر قله.** `CLAUDE.md` می‌گوید ناحیه جایی است که حجم **کم** است
   (LVN — جایی که فروشنده نبوده). کد سه بین **پرحجم‌ترین** را می‌گیرد. این دو
   مفهوم مخالف‌اند.
2. **تعداد ردیف ثابت است.** `CLAUDE.md` صریحاً می‌گوید «تعریف ۶ ردیف ثابت
   **غلط** است» و حلقهٔ `for(var nb=3; nb<=20; nb++)` را به‌عنوان پیاده‌سازی
   مرجع نقل می‌کند. آن حلقه **در کد نیست**؛ `nb` یک‌بار حساب می‌شود.
3. **بین‌بندی روی close.** بند ۴ همان فایل این را به‌عنوان «اشتباه شمارهٔ ۱ که
   تصحیح شد» ثبت کرده. هنوز تصحیح نشده.

سنجیدمشان: `tools/verify_box.py` هر دو تعریف را روی ۵۰۰ پنجرهٔ یکسان اجرا کرد —
**در ۱۰۰٪ موارد ناحیهٔ متفاوت دادند.**

پورت دره در `tools/vp_box.py` در برابر رونویسی مستقل از Pine تست تفاضلی شد:
**۴۰۰۰ مورد، صفر اختلاف** + ۴ ادعای معنایی.

### کدام دره؟ — کمتر از آنچه فکر می‌کردم اهمیت دارد

Pine از **پایین به بالا** می‌گردد و **اولین** کمینهٔ محلی میانی را برمی‌دارد —
یعنی پایین‌ترین دره، نه نزدیک‌ترین دره به قیمت.

در اسکرین‌شات کهربا، ردیف‌ها از پایین: ۷۹۲٫۶۱M، ۳۵٫۱۹M، ۱۶۰٫۰۵M، ۱۹۹٫۹۴M،
۲۱۳٫۴۳M، ۶۸٫۹۱M، ۸۵٫۷۱M، ۲۹٫۳۸M. اولین کمینهٔ میانی از پایین **۳۵٫۱۹M** است
(~۲۱۴٬۰۰۰)، ولی ناحیهٔ نارنجی روی **۶۸٫۹۱M** (~۲۲۴٬۰۰۰) نشسته.

اول فکر می‌کردم این مهم‌ترین تصمیم بلوکه‌کننده است. اندازه گرفتم و **نبود**.
`tools/verify_box.py` روی ۵۹۲ پنجرهٔ تصادفی:

```
حلقه کجا می‌ایستد: 4 ردیف: 23٪، 5 ردیف: 20٪، 6 ردیف: 17٪، 3 ردیف: 16٪
پروفایل با فقط یک دره: 94٪
اختلاف انتخاب‌گرها: اولین≠نزدیک‌ترین 3٪، اولین≠عمیق‌ترین 3٪
```

دلیلش ساده است: حلقه روی **اولین** شمارش ردیفی می‌ایستد که دره بدهد، و آن
معمولاً ۳ تا ۶ است. با ۳ ردیف فقط یک ایندکس میانی وجود دارد، پس حداکثر یک دره
ممکن است. در ۹۴٪ موارد اصلاً انتخابی در کار نیست — یک دره هست و همه همان را
برمی‌دارند.

پس تصحیح می‌کنم: **«دره یا سه بین پرحجم» تصمیم بزرگ است (۱۰۰٪ اختلاف)، ولی
«کدام دره» تصمیم کوچکی است (~۳٪ اختلاف).**

### پیامدی که بزرگ‌تر است: عرض باکس

اگر حلقه روی ۳ تا ۶ ردیف بایستد، باکس **یک‌سوم تا یک‌ششم کل دامنهٔ ماه** است.
باکس‌های داشبورد فعلی ۰٫۴۷٪ تا ۱٫۳۸٪ عرض دارند — چند برابر باریک‌تر از چیزی که
تعریف دره می‌دهد.

این با مسیر `value_area` می‌خواند: با `nb = min(20, max(5, len/2))` و یک ماه
~۲۰ کندل، `nb=20` می‌شود و هر بین ۵٪ از دامنهٔ close است. برای کهربا با باکس
۳۰۸۰ واحدی و سه بین مجاور، دامنهٔ close ماه حدود ۹٪ درمی‌آید — کاملاً محتمل.

پیامد عملی: با تعریف دره، «ریسک» کهربا از **۰٫۶۴٪** به چند درصد می‌پرد، و فیلتر
`RISK_RELAXED = 7.0` رفتار کاملاً متفاوتی پیدا می‌کند. `monthly_backtest.py`
ستون عرض باکس هر تعریف را چاپ می‌کند تا روی دادهٔ واقعی مقایسه‌شدنی باشد.

---

## ۳. هندسهٔ معامله با بازده گزارش‌شده نمی‌خواند

`CLAUDE.md` بند ۱: ورود روی سقف باکس، استاپ زیر کف باکس، تارگت به اندازهٔ ارتفاع
باکس → ذاتاً **۱:۱**.

ولی باکس‌های خروجی بسیار باریک‌اند:

| نماد | باکس | عرض |
|---|---|---|
| زیتون | ۴۳۳۰۷–۴۳۵۱۲ | ۰٫۴۷٪ |
| تمشک | ۴۹۲۷۰–۴۹۵۳۱ | ۰٫۵۳٪ |
| کهربا | ۲۲۳۳۶۰–۲۲۶۴۴۰ | ۱٫۳۸٪ |

با هندسهٔ ۱:۱ روی باکس ۱٪، میانگین بازده هر معامله حداکثر حدود ۱٪ است — نه
**‎+۱۲٫۳۴٪**. یعنی `dashboard_monthly.py` اصلاً هندسهٔ باکس را اجرا نمی‌کند؛ دارد
«یک ماه نگه دار» را اندازه می‌گیرد. آن هم یک استراتژی است، ولی **استراتژی شما
نیست** و بازدهش عمدتاً بازده خود بازار است.

---

## ۴. باگ‌های مشخص

### الف) برخورد نماد — دوآیکس دادهٔ آگاس را گرفته

```
آگاس    | کلوز 495201.0 | جاری 469516.0-492344.0 | ریسک 0.58% | قبل 8.83%
دوآیکس  | کلوز 495201.0 | جاری 469516.0-492344.0 | ریسک 0.58% | قبل 8.83%
```

ریال‌به‌ریال یکسان. دوآیکس نماد جداگانه‌ای است (`دوایکس` در همان خروجی کلوز
۳۷۹۴۴ دارد). حل‌کنندهٔ نام، دوآیکس را به آگاس رسانده.

### ب) نماد تکراری

`دارا یکم` و `دارایکم` هر دو حاضرند با اعداد یکسان — یک نماد، دو ردیف، دو بار
وزن در میانگین.

### ج) ورود روی «قیمت پایانی» اجراشدنی نیست

هر چهار اسکریپت ورود را روی `close` همان روز می‌گیرند. در بورس ایران قیمت
پایانی **میانگین وزنی** است، نه قیمتی که بتوان رویش معامله کرد. `CLAUDE.md`
بند ۲ این را درست حل کرده («سبد دیلی در پیش‌گشایش می‌خرد»)، ولی کد پایتون
نکرده. بازده بک‌تست به همین اندازه خوش‌بینانه است.

### د) تریگری که خودتان ردش کرده‌اید

`CLAUDE.md` واگرایی RSI ۱۵ دقیقه را رد کرده (۴۰٪ برد در برابر پایهٔ ۴۳٫۵٪).
`backtest_auto_nofixed.py` و `daily_etf_signal_bot.py` هنوز آن را به‌عنوان
تریگر ورود به کار می‌برند.

### ه) انتخاب نماد از روی همان داده

```python
# backtest_filtered_exit.py
# فقط نمادهای با موفقیت بالاتر در بک‌تست قبلی
FILES = {'jhsh': 'جهش', 'doaiks': 'دوآیکس', ...}
```

نمادها از روی نتیجهٔ بک‌تست قبلی روی **همان داده** انتخاب شده‌اند. هر عددی که
این فایل بدهد بی‌معناست.

### و) کد مرده

`brsapi_download_backtest.py` خطوط ۲۰۷–۲۱۰:

```python
trig = gap or (...)                      # ساخته می‌شود، هرگز استفاده نمی‌شود
ok = low_risk and above and (gap or True)   # `gap or True` همیشه True
ok = low_risk and above and (gap or mr <= 4.0)   # این یکی برنده است
```

### ز) `minRow` در Pine

ورودی `minRow` مقدار `minval=2` را می‌پذیرد. حلقهٔ `for i = 1 to rows - 2` در
Pine وقتی پایان کمتر از شروع باشد **رو به پایین** می‌شمارد و به `array.get(bins,
-1)` می‌رسد → خطای زمان اجرا. با پیش‌فرض ۳ رخ نمی‌دهد. در پورت پایتون بسته شد.

---

## ۵. چه چیزی درست است

- ساختار «باکس دورهٔ کامل‌شدهٔ قبل → تصمیم روی کلوز اول دورهٔ بعد» منطق قفل
  درستی دارد و لوک‌اهد ندارد.
- `include_today skipped` در `algotik_tse` یعنی دادهٔ امروز وارد نمی‌شود — خوب است.
- ناحیهٔ محاسبه‌شده برای کهربا با آنچه چارتیکس نشان می‌دهد می‌خواند، پس پیاده‌سازی
  با ابزار بصری هم‌خوان است (هرچند تعریفش آن چیزی نیست که `CLAUDE.md` می‌گوید).
- یافته‌های تأییدشدهٔ `CLAUDE.md` (قدرت خریدار، پولبک هفتگی با n=۴۲۹ و t=۹٫۹۱)
  با نرخ پایه سنجیده شده‌اند و از جنس دیگری‌اند. این بازبینی آن‌ها را زیر سؤال
  نمی‌برد — فقط `dashboard_monthly.py` را.

---

## ۶. چرا t معمولی اینجا کار نمی‌کند

۱۴۵ نماد در یک ماه، یک بازار را معامله می‌کنند. وقتی شاخص بالا می‌رود تقریباً
همه با هم بالا می‌روند. پس ۶۱۵ «مشاهده» در عمل ۶ مشاهدهٔ مستقل است و هر t که
با `n=615` حساب شود چند برابر بزرگ‌تر از واقعیت درمی‌آید.

`monthly_backtest.py` به‌جایش **تست جایگشت** می‌زند: برچسب «بالا» را **داخل هر
ماه** به‌هم می‌ریزد، به‌طوری که تعداد سیگنال آن ماه و حرکت کل بازار در آن ماه
دست‌نخورده بماند، و فقط می‌پرسد «آیا باکس نمادهای بهتری از تصادف انتخاب کرد؟»

روی دادهٔ تصادفی ساختگی (بدون هیچ سیگنالی):

```
تعریف              ماه   مزیت واحد٪        p
valley_first        16       +0.71   0.1674
valley_nearest      15       +0.53   0.2249
valley_deepest      16       +0.49   0.2754
value_area          13       +0.95   0.1844
```

هیچ‌کدام زیر ۰٫۰۵ نیامد — یعنی تست مثبت کاذب نمی‌دهد. در همان اجرا Welch برای
`value_area` عدد `t=+1.70` داد که وسوسه‌کننده به نظر می‌رسد ولی جایگشت ردش کرد.

⚠️ **تصحیح یک خطای خودِ این سند.** نسخه‌های قبلی نوشته بودند «با `m` ماه،
کمترین p ممکن حدود `1/(m+1)` است، پس هیچ سیگنالی با این حجم داده به p<0.05
نمی‌رسد». **این غلط بود.** آن کران برای تستی است که علامت کل یک ماه را وارونه
می‌کند (sign-flip ماه‌به‌ماه). تست اینجا آن نیست: برچسب سیگنال **داخل** هر ماه
جابه‌جا می‌شود و هر ماه ده‌ها نماد دارد، پس فضای جایگشت نجومی است
(برای ۹ ماه از دادهٔ واقعی حدود ۱۰^۲۳۲ حالت) و کف p فقط `۱/(تکرار+۱)` است —
با ۲۰٬۰۰۰ تکرار یعنی ۰٫۰۰۰۰۵.

آنچه تعداد ماه محدود می‌کند **تعمیم‌پذیری** است، نه تفکیک‌پذیری: ۹ ماه یک
رژیم بازار است. p کوچک می‌گوید باکس **در این دوره** بهتر از تصادف انتخاب
کرده، نه اینکه همیشه می‌کند.

---

## ۷. هندسهٔ ۱:۱ هم اجرا شد

`geometry_trade()` همان چیزی را پیاده می‌کند که `CLAUDE.md` بند ۱ می‌گوید: ورود
روی پولبک به سقف باکس، استاپ کف باکس، تارگت به اندازهٔ ارتفاع باکس، و اگر پولبک
نخورد «حمایت خالی» است و معامله‌ای نیست.

روی دادهٔ ساختگی نرخ «حمایت خالی» حدود ۳۵٪ درآمد — نزدیک به ۵۰٪ که `CLAUDE.md`
برای ماهانه ثبت کرده.

**قید مهم:** همان اجرا میانگین `+0.326R` داد روی دادهٔ بدون سیگنال. علتش دریفت
مثبت دادهٔ ساختگی و انتخابی‌بودن ورود روی پولبک است. یعنی این سنجه هم **گروه
کنترل خودش را لازم دارد** و عدد خامش را نباید مزیت خواند. فعلاً برای مقایسهٔ
نسبی بین تعریف‌ها مفید است، نه برای ادعای سودآوری.

---

## ۸. دادهٔ واقعی رسید — و جواب داد

۱۴۶ فایل CSV روزانه (`data_auto`)، ۱۳۸ نماد یکتا، ۲۰۲۵/۱۱ تا ۲۰۲۶/۰۹.
این بخش، بخش‌های بالا را **جایگزین** می‌کند هرجا که با آن‌ها بخواند.

### ۸٫۱ پورت باکس درست است — ۱۱ از ۱۱، تا ریال

`make_box("valley_first", ...)` روی کندل‌های ماه جاری، دقیقاً همان
«لو_جاری/های_جاری» داشبورد خودتان را می‌دهد:

| نماد | داشبورد شما | پورت اینجا |
|---|---|---|
| نقران | ۱۲٬۷۳۵–۱۲٬۸۹۹ | ۱۲٬۷۳۵–۱۲٬۸۹۹ |
| کهربا | ۲۲۳٬۳۶۰–۲۲۶٬۴۴۰ | ۲۲۳٬۳۶۰–۲۲۶٬۴۴۰ |
| دوایکس | ۳۱٬۶۱۲–۳۶٬۶۶۳ | ۳۱٬۶۱۲–۳۶٬۶۶۳ |
| طلا | ۱٬۶۴۶٬۰۳۱–۱٬۶۶۷٬۵۳۹ | ۱٬۶۴۶٬۰۳۱–۱٬۶۶۷٬۵۳۹ |

یعنی `dashboard_monthly.py` شما **همان تعریف دره** را اجرا می‌کند، نه
`box()` سه‌بین‌پرحجمِ `dash.html`. بخش ۲ این سند بر اساس `dash.html` نوشته
شده بود؛ برای داشبورد تازه‌تان آن ایراد وارد نیست.

### ۸٫۲ تعریف دره از آزمون رد می‌شود، سه‌بین‌پرحجم نه

پنجرهٔ ۱ — باکس از ماه قبلِ کامل‌شده، بازده کل ماه بعد. ۹ ماه قابل‌استفاده،
۲۰٬۰۰۰ تکرار، بدون درآمد ثابت:

| تعریف | سیگنال | نرخ پایه | برد | مزیت واحد٪ | p |
|---|---|---|---|---|---|
| **اولین دره** | ۵۲۸ | ۶۷٫۰٪ | ۷۶٫۹٪ | **+۰٫۷۶** | **۰٫۰۰۰۴** ★ |
| عمیق‌ترین دره | ۵۲۶ | ۶۷٫۰٪ | ۷۷٫۰٪ | +۰٫۷۴ | ۰٫۰۰۰۷ ★ |
| نزدیک‌ترین دره | ۵۲۳ | ۶۷٫۰٪ | ۷۶٫۹٪ | +۰٫۷۴ | ۰٫۰۰۱۰ ★ |
| سه‌بین پرحجم | ۳۲۱ | ۶۷٫۰٪ | ۸۲٫۲٪ | +۰٫۵۴ | ۰٫۱۰۶۵ |

دقت کنید سه‌بین‌پرحجم **بالاترین نرخ برد** را دارد (۸۲٫۲٪) و در آزمون **رد
می‌شود**. این همان چیزی است که بخش ۱ می‌گفت: نرخ برد بدون نرخ پایه، عدد نیست.

### ۸٫۳ ولی چراغ زنده — همان که در طول ماه نگاه می‌کنید — شواهد ندارد

پنجرهٔ ۲: باکس از کندل‌های **همین ماه تا امروز**، بازده **از امروز تا پایان
همین ماه**. این دقیقاً چراغی است که داشبورد در طول ماه نشان می‌دهد.
۱۰٬۵۸۳ مشاهده (نماد × روز)، ۱۲۵ نماد، ۱۲ ماه. نرخ پایه: +۲٫۲۹٪، مثبت ۶۱٫۷٪.

خام، روی کل نمونه، خیلی قانع‌کننده به نظر می‌رسد:

| حالت امروز | n | مثبت | میانگین تا پایان ماه | مزیت |
|---|---|---|---|---|
| بالای باکس | ۶٬۵۱۷ | ۶۷٫۰٪ | +۳٫۱۳٪ | +۰٫۸۴ |
| داخل باکس | ۱٬۰۱۹ | ۶۱٫۲٪ | +۱٫۲۷٪ | −۱٫۰۲ |
| زیر باکس | ۳٬۰۴۷ | ۵۰٫۷٪ | +۰٫۸۴٪ | −۱٫۴۵ |

**ولی این عدد خام است.** دو چیز را کنترل نکرده: کدام ماه، و کدام روزِ ماه
(چون هرچه دیرتر در ماه، افق تا کلوز کوتاه‌تر). افق خودش مقصر نیست — میانگین
روزِ ماه برای هر سه حالت یکی است (۱۱٫۸ و ۱۲٫۱ و ۱۱٫۹). ولی وقتی مقایسه را
**داخل هر سلولِ (ماه × روزِ ماه)** ببریم، یعنی فقط نمادهایی را با هم مقایسه
کنیم که در یک روزِ یکسان از یک ماهِ یکسان‌اند:

| حالت | سلول | n | مزیت واحد٪ | خطای معیار | t |
|---|---|---|---|---|---|
| بالای باکس | ۱۲۴ | ۶٬۴۷۶ | **+۰٫۰۱** | ۰٫۱۷ | +۰٫۰۶ |
| داخل باکس | ۸۶ | ۹۷۷ | −۰٫۲۲ | ۰٫۲۸ | −۰٫۷۹ |
| زیر باکس | ۱۰۶ | ۳٬۰۲۱ | −۰٫۴۷ | ۰٫۳۲ | −۱٫۴۸ |

آزمون جایگشتِ درون‌ماه هم برای «بالای باکس» همین را می‌گوید: مزیت **منفی**
درمی‌آید (−۰٫۴۳ واحد درصد، p=۱٫۰۰).

**ولی برای «زیر باکس» سه مشخصهٔ معقول، سه جواب می‌دهند — و علامتشان یکی
نیست:**

| چطور حساب شد | چه چیزی کنترل شده | مزیت «زیر باکس» |
|---|---|---|
| خام، روی کل نمونه | هیچ | **−۱٫۴۵** |
| جایگشت، داخل هر ماه | ماه | **+۰٫۳۱** (p=۰٫۰۵۵) |
| داخل هر (ماه × روزِ ماه) | ماه و افق | **−۰٫۴۷** (t=−۱٫۴۸) |

هیچ‌کدام به آستانه نمی‌رسد، و مهم‌تر از آن: **علامت با انتخاب مشخصه عوض
می‌شود.** وقتی یک برآورد با تغییر روشِ کنترل از ۱٫۴۵− به ۰٫۳۱+ می‌پرد، آن
برآورد چیزی را اندازه نمی‌گیرد؛ نویز را اندازه می‌گیرد. (دو آزمون آخر به این
دلیل فرق می‌کنند که جایگشتِ ماهانه همهٔ روزهای یک ماه را در یک استخر می‌ریزد و
هر مشاهده را برابر وزن می‌دهد، ولی آزمون سلولی به هر سلول وزن برابر می‌دهد.)

**نتیجه:** آن ۰٫۸۴+ و ۱٫۴۵− در جدول خام، ترکیبِ ماه بود، نه انتخابِ نماد.
«بالای باکس» در هر سه مشخصه صفر است (+۰٫۰۱ با کنترل کامل). «زیر باکس» ناپایدار
است. **چراغ زنده در این داده اطلاعاتی حمل نمی‌کند که از تصادف قابل تفکیک
باشد** — نه به این معنا که اثر ندارد، به این معنا که این حجم داده نمی‌تواند
نشانش بدهد.

### ۸٫۴ پس چه چیزی شواهد دارد و چه چیزی ندارد

| | شواهد | مزیت | p |
|---|---|---|---|
| باکس **ماه قبل** → بازده ماه بعد | **دارد** | +۰٫۷۶ واحد٪ | ۰٫۰۰۰۴ |
| باکس **ماه جاری تا امروز** → بازده تا پایان ماه | **ندارد** | +۰٫۰۱ واحد٪ | — |

این دو تناقض نیستند؛ دو پنجره‌اند. و ترتیبشان دقیقاً همان چیزی است که
`CLAUDE.md` بند ۱ می‌گوید: **تصمیم روی کلوز پایان ماه گرفته می‌شود.** قاعدهٔ
خودتان همان قاعده‌ای است که شواهد دارد. چراغ وسط ماه، هرقدر هم قرمز، در این
یازده ماه چیزی را جدا نکرده.

⚠️ یک نامتقارنی که باید دید: پنجرهٔ ۱ در هر سه انتخاب‌گرِ دره پایدار است
(+۰٫۷۶ و +۰٫۷۴ و +۰٫۷۴، هر سه p<0.002) ولی پنجرهٔ ۲ حتی علامتش با مشخصه عوض
می‌شود. پایداری خودش شاهد است — به همان اندازهٔ p.

⚠️ **قید:** «شواهد ندارد» یعنی «در این ۱۲ ماه و این ۱۲۵ نماد، از تصادف جدا
نشد». یعنی نبودِ اثباتِ اثر، نه اثباتِ نبودِ اثر. با دادهٔ بیشتر ممکن است
تغییر کند.

### ۸٫۵ نقدشوندگی قید این پرتفو نیست

نسبتِ واحدمحور (مستقل از مقیاسِ مشکوکِ ریالی)، میانهٔ حجم ۲۰ روز، مشارکت ۲۰٪:

| نماد | ٪ حجم یک روز | روزِ خروج |
|---|---|---|
| نقران | ۲٫۴۶٪ | ۰٫۱۲ |
| دوایکس | ۰٫۳۸٪ | ۰٫۰۲ |
| کهربا | ۰٫۲۷٪ | ۰٫۰۱ |
| بیدار | ۰٫۲۰٪ | ۰٫۰۱ |
| موج | ۰٫۰۷٪ | ۰٫۰۰ |

بزرگ‌ترین پوزیشن کمتر از یک‌هشتمِ یک روزِ معاملاتی است. یعنی «امشب یا فردا»
هیچ‌وقت سؤالِ اجرا نبوده — هر تصمیمی اینجا تصمیمِ قاعده است، نه تصمیمِ
نقدشوندگی.

---

## ۹. ترتیب کارها — به‌روزشده

سه مورد از فهرست قبلی **انجام شد** و جوابشان در بخش ۸ است:

- ~~دره یا سه‌بین پرحجم؟~~ → **دره.** p=۰٫۰۰۰۴ در برابر ۰٫۱۰۶۵.
- ~~`monthly_backtest.py` را روی دادهٔ واقعی اجرا کنید.~~ → اجرا شد، ۱۳۸ نماد.
- ~~جدول عرض باکس را مقایسه کنید.~~ → دره ۴٫۱۸٪، سه‌بین ۹٫۴۹٪؛ و پورت،
  باکس زندهٔ داشبورد شما را ۱۱ از ۱۱ بازتولید کرد.

و یک مورد **تصحیح** شد: «با ۶ ماه هیچ سیگنالی به p<0.05 نمی‌رسد» غلط بود.
بخش ۶ را ببینید.

آنچه می‌ماند:

۱. **دادهٔ بیشتر، ولی نه به آن دلیلی که فکر می‌کردیم.** ۱۱ ماه برای تفکیک کافی
   بود؛ برای **تعمیم** کافی نیست. این یازده ماه یک رژیم بازار بوده که در آن
   نرخ پایه ۶۷٪ مثبت است. آنچه نمی‌دانیم این است که در رژیم نزولی چه می‌شود.
۲. **چراغ وسط ماه را از جایگاه تصمیم بیرون بیاورید** یا دست‌کم علامتش بزنید.
   بخش ۸٫۳: در این داده اطلاعاتی حمل نمی‌کند. ستون «وضعیت ماه قبل» آن است که
   شواهد دارد.
۳. `box()` در `dash.html` را با تعریف نهایی یکی کنید، یا کامنتش را تصحیح کنید
   تا ادعای خلاف `CLAUDE.md` نکند. (`dashboard_monthly.py` تازه‌تان درست است.)
۴. صندوق درآمد ثابت را از جهان نماد بیرون بگذارید.
۵. برخورد دوآیکس/آگاس و تکرار دارا یکم را پاک کنید.
۶. ورود را به پیش‌گشایش روز بعد منتقل کنید، نه قیمت پایانی.
۷. **کلید BrsApi را ابطال کنید.** هنوز انجام نشده.
